[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "offline-morse-generator"
version = "6.0.0"
requires-python = ">=3.13"
dependencies = ["lameenc==1.8.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
